export interface EcmaFeatures {
  /* Enabling `return` statements at the top-level. */
  globalReturn?: boolean
  /* Enabling implied strict mode. */
  impliedStrict?: boolean
  /* Enabling JSX syntax. */
  jsx?: boolean
}
