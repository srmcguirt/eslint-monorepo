import { eslintPluginImport } from './eslint-plugin-import'

describe('eslintPluginImport', () => {
  it('should work', () => {
    expect(eslintPluginImport()).toEqual('eslint-plugin-import')
  })
})
