import { defineConfig } from "../eslint-config"

export default defineConfig({
  files: [],
  ignores: [],
  languageOptions: {},
  linterOptions: {},
  processor: {},
  plugins: [],
  rules: {},
  settings: {},
})

